# Alpha3 feature scope

Planned: direct MCP file/image artifact return, graphical screenshot return, and strict key-only OpenSSH client tools with explicit host-key verification. Production deployment is deferred until code and CI validation complete.
